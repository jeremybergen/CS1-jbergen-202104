#include <iostream>
#include <string>

using namespace std;

int main() {
    int num1 = 6;
    
    cout << num1 << endl;

    num1 = num1 << 1;
    // num1 = num1 * 2;

    cout << num1 << endl;

    num1 = num1 << 1;

    cout << num1 << endl;

    num1 = num1 >> 1;

    cout << num1 << endl;
    
    num1 = num1 >> 1;

    cout << num1 << endl;

    num1 = num1 >> 1;

    cout << num1 << endl;

    num1 = num1 >> 1;

    cout << num1 << endl;

    num1 = num1 >> 1;

    cout << num1 << endl;

    num1 = num1 >> 1;

    cout << num1 << endl;
    // int input_seconds = 12345;
    // int seconds_in_hour = 3600;
    // int seconds_in_minute = 60;

    // cout << "10 / 0 = " << 10/0 << endl;
    // int hours = input_seconds / seconds_in_hour;
    // int minutes = (input_seconds - (hours * seconds_in_hour)) / seconds_in_minute;
    // int seconds = (input_seconds - (hours * seconds_in_hour)) % seconds_in_minute;

    // cout << "There are " << hours << " hours, " 
    //      << minutes << " minutes, " 
    //      << seconds << " seconds in " 
    //      << input_seconds << " seconds." << endl; 
    // int seconds = (input_seconds - (hours * hours_in_seconds)) - minutes;
    // int answer = 0;
    // string opp = "";

    // answer = num1 % num2;
    // opp = " % ";

    // cout << num1 << opp 
    //      << num2 << " = " 
    //      << answer << endl;

    return 0;
}